import { useState, useEffect } from 'react';
import { get{{pascalCaseName}} } from '../utils';
import type { {{pascalCaseName}}RendererProps } from '../{{pascalCaseName}}.types';

export function use{{pascalCaseName}}(): Omit<{{pascalCaseName}}RendererProps, 'className'> {
  const [data, setData] = useState('');
  const [isLoading, setIsLoading] = useState(true);

  useEffect(() => {
    const fetchData = async () => {
      const result = await get{{pascalCaseName}}();
      setData(result);
      setIsLoading(false);
    };

    fetchData();
  }, []);

  return { data, isLoading };
}
